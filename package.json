{
  "name": "referkit",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "pnpm": {
    "overrides": {
      "drizzle-orm": "^0.30.0"
    }
  },
  "packageManager": "pnpm@8.15.0"
}
