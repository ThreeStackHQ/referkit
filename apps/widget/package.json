{
  "name": "@referkit/widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=iife --global-name=ReferKit --outfile=dist/widget.js --target=es2017",
    "dev": "esbuild src/index.ts --bundle --format=iife --global-name=ReferKit --outfile=dist/widget.dev.js --target=es2017 --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.0.0"
  }
}
